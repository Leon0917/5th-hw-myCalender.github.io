$(document).ready(function() {
    $(".saveBtn") on(click, function {
        var = $(this).sibling(".info").val();
        var = $(this).parent().attr("id");
        console.log(info)
    })
})

localStorage.setItem(time, value);
